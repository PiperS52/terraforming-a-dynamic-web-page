# terraforming-a-dynamic-web-page
IaC to provision a dynamic web page with AWS API Gateway + AWS Lambda.


## Running

```bash
$ zip lambda_function.zip lambda_function.py
$ terraform init
$ terraform plan
$ terraform apply
```